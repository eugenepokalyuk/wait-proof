# Скопировать в .env.local. Локально бэкенд — make runserver в api/.
NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1
# Пусто локально; на GitHub Pages — /wait-proof (ставит workflow).
NEXT_PUBLIC_BASE_PATH=
